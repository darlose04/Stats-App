<% include ./partials/header %>

<div class="ui grid">
    <div class="ui four column grid">
            <div class="row">
                <% images.forEach(function(image) { %>
                    <div class="column"><img src="<%= image.image %>" alt=""></div>
                <% }) %>
            </div>
    </div>
</div>


<% include ./partials/footer %>